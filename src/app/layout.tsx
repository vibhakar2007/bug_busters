import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BugBusters • Technical Symposium Quiz Platform",
  description: "Think fast. Debug faster. Competitive technical symposium quiz platform with anti-cheating telemetry and live admin monitoring.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#070916] text-white font-sans selection:bg-[#9db40c] selection:text-[#070916]">
        {children}
      </body>
    </html>
  );
}
