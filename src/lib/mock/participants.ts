import { Participant } from '@/types/participant';
import participantsData from '@/data/participants.json';

export const MOCK_PARTICIPANTS: Participant[] = participantsData as unknown as Participant[];
